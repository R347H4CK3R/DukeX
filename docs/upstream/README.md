# Upstream Reference Files

This folder keeps upstream QEMU reference material that is useful for
provenance, history, and contributor context, but is not part of the DukeX iOS
build path.

- `QEMU_README.rst`: original upstream QEMU README.
- `MAINTAINERS`: upstream subsystem ownership metadata.
- `mailmap`: upstream contributor mailmap.
